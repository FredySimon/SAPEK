import { AsignarCursoPipe } from './asignar-curso.pipe';

describe('AsignarCursoPipe', () => {
  it('create an instance', () => {
    const pipe = new AsignarCursoPipe();
    expect(pipe).toBeTruthy();
  });
});
