import { AsignarInstructorPipe } from './asignar-instructor.pipe';

describe('AsignarInstructorPipe', () => {
  it('create an instance', () => {
    const pipe = new AsignarInstructorPipe();
    expect(pipe).toBeTruthy();
  });
});
