import { AsignacionCarrerasPipe } from './asignacion-carreras.pipe';

describe('AsignacionCarrerasPipe', () => {
  it('create an instance', () => {
    const pipe = new AsignacionCarrerasPipe();
    expect(pipe).toBeTruthy();
  });
});
