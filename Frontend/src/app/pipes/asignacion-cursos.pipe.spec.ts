import { AsignacionCursosPipe } from './asignacion-cursos.pipe';

describe('AsignacionCursosPipe', () => {
  it('create an instance', () => {
    const pipe = new AsignacionCursosPipe();
    expect(pipe).toBeTruthy();
  });
});
